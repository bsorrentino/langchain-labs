version: '3'
services:
  langchain-streamlit-agent:
    image: langchain-streamlit-agent:latest
    build: .
    #command: streamlit run src/basic_memory.py --server.port 8051
    volumes:
      - ./src/:/app/src
    ports:
      - 8051:8051